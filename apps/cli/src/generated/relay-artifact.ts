// Generated by scripts/embed-relay.mjs before CLI builds.
export const EMBEDDED_RELAY_SHA256 = "f9f9a35c8a263b935731c028ecd2f8eafcff22a2a11dd650c6c197e9036a4cf4";
export const EMBEDDED_RELAY_SOURCE = "// ../../packages/contracts/dist/findings.js\nvar FINDING_DEFECT_KINDS = [\n  \"correctness\",\n  \"validation\",\n  \"resource-lifecycle\",\n  \"concurrency\",\n  \"security\",\n  \"privacy\",\n  \"performance\",\n  \"architecture\",\n  \"compatibility\",\n  \"error-handling\",\n  \"test-coverage\"\n];\nvar FINDING_IMPACT_KINDS = [\n  \"incorrect-result\",\n  \"operation-failure\",\n  \"data-loss\",\n  \"resource-leak\",\n  \"execution-stall\",\n  \"security-exposure\",\n  \"privacy-exposure\",\n  \"performance-degradation\",\n  \"boundary-violation\",\n  \"compatibility-break\",\n  \"regression-risk\"\n];\nvar FINDING_FIX_ACTIONS = [\n  \"guard\",\n  \"validate\",\n  \"preserve\",\n  \"propagate\",\n  \"synchronize\",\n  \"release\",\n  \"minimize\",\n  \"optimize\",\n  \"decouple\",\n  \"restore\",\n  \"add-test\"\n];\nvar DEFECT_KINDS = new Set(FINDING_DEFECT_KINDS);\nvar IMPACT_KINDS = new Set(FINDING_IMPACT_KINDS);\nvar FIX_ACTIONS = new Set(FINDING_FIX_ACTIONS);\n\n// ../../packages/contracts/dist/review-job.js\nvar REVIEW_QUEUE_JOB_CONTRACT_VERSION = 1;\nvar REVIEW_JOB_ACTIONS = [\n  \"opened\",\n  \"reopened\",\n  \"ready_for_review\",\n  \"synchronize\"\n];\nvar ReviewJobSchemaError = class extends Error {\n  constructor(message, options) {\n    super(message, options);\n    this.name = \"ReviewJobSchemaError\";\n  }\n};\nvar DELIVERY_ID = /^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/u;\nvar GITHUB_OWNER = /^[A-Za-z0-9](?:[A-Za-z0-9-]{0,38})$/u;\nvar GITHUB_REPOSITORY = /^[A-Za-z0-9._-]+$/u;\nvar SHA = /^[0-9a-f]{40}$/u;\nvar TOP_LEVEL_FIELDS = [\n  \"version\",\n  \"deliveryId\",\n  \"installationId\",\n  \"repository\",\n  \"pullRequest\",\n  \"trigger\",\n  \"enqueuedAt\"\n];\nvar REPOSITORY_FIELDS = [\"id\", \"owner\", \"name\"];\nvar PULL_REQUEST_FIELDS = [\"number\"];\nvar AUTOMATIC_TRIGGER_FIELDS = [\n  \"kind\",\n  \"action\",\n  \"authorId\",\n  \"senderId\",\n  \"baseRepositoryId\",\n  \"headRepositoryId\",\n  \"baseSha\",\n  \"headSha\"\n];\nvar REQUESTED_TRIGGER_FIELDS = [\"kind\", \"source\", \"commentId\", \"senderId\"];\nfunction record(value, path) {\n  if (typeof value !== \"object\" || value === null || Array.isArray(value)) {\n    throw new ReviewJobSchemaError(`${path} must be an object.`);\n  }\n  return value;\n}\nfunction checkKeys(value, path, expected) {\n  if (Object.keys(value).length !== expected.length || Object.keys(value).some((key) => !expected.includes(key))) {\n    throw new ReviewJobSchemaError(`${path} must contain exactly the expected fields.`);\n  }\n}\nfunction string(value, path) {\n  if (typeof value !== \"string\" || value.length === 0) {\n    throw new ReviewJobSchemaError(`${path} must be a non-empty string.`);\n  }\n  return value;\n}\nfunction positiveInteger(value, path) {\n  if (!Number.isSafeInteger(value) || value <= 0) {\n    throw new ReviewJobSchemaError(`${path} must be a positive integer.`);\n  }\n  return value;\n}\nfunction candidate(value) {\n  if (typeof value !== \"string\") {\n    return value;\n  }\n  try {\n    return JSON.parse(value);\n  } catch (error) {\n    throw new ReviewJobSchemaError(\"Review job must be valid JSON.\", { cause: error });\n  }\n}\nfunction parseDeliveryId(value) {\n  const parsed = string(value, \"review job.deliveryId\");\n  if (!DELIVERY_ID.test(parsed)) {\n    throw new ReviewJobSchemaError(\"review job.deliveryId is malformed.\");\n  }\n  return parsed;\n}\nfunction parseRepository(value) {\n  const parsed = record(value, \"review job.repository\");\n  checkKeys(parsed, \"review job.repository\", REPOSITORY_FIELDS);\n  const id = positiveInteger(parsed.id, \"review job.repository.id\");\n  const owner = string(parsed.owner, \"review job.repository.owner\");\n  if (!GITHUB_OWNER.test(owner)) {\n    throw new ReviewJobSchemaError(\"review job.repository.owner is malformed.\");\n  }\n  const name = string(parsed.name, \"review job.repository.name\");\n  if (!GITHUB_REPOSITORY.test(name)) {\n    throw new ReviewJobSchemaError(\"review job.repository.name is malformed.\");\n  }\n  return { id, owner, name };\n}\nfunction parseEnqueuedAt(value) {\n  const parsed = string(value, \"review job.enqueuedAt\");\n  if (!parsed.endsWith(\"Z\") || Number.isNaN(Date.parse(parsed)) || new Date(parsed).toISOString() !== parsed) {\n    throw new ReviewJobSchemaError(\"review job.enqueuedAt must be a canonical UTC timestamp.\");\n  }\n  return parsed;\n}\nfunction parseAutomaticTrigger(value, repositoryId) {\n  checkKeys(value, \"review job.trigger\", AUTOMATIC_TRIGGER_FIELDS);\n  const action = string(value.action, \"review job.trigger.action\");\n  if (!REVIEW_JOB_ACTIONS.includes(action)) {\n    throw new ReviewJobSchemaError(\"review job.trigger.action is not supported.\");\n  }\n  const baseRepositoryId = positiveInteger(value.baseRepositoryId, \"review job.trigger.baseRepositoryId\");\n  const headRepositoryId = positiveInteger(value.headRepositoryId, \"review job.trigger.headRepositoryId\");\n  if (baseRepositoryId !== repositoryId || headRepositoryId !== repositoryId) {\n    throw new ReviewJobSchemaError(\"review job repository identities must describe one same-repository pull request.\");\n  }\n  const baseSha = string(value.baseSha, \"review job.trigger.baseSha\");\n  const headSha = string(value.headSha, \"review job.trigger.headSha\");\n  if (!SHA.test(baseSha) || !SHA.test(headSha)) {\n    throw new ReviewJobSchemaError(\"review job revisions must be lowercase 40-character SHAs.\");\n  }\n  return {\n    kind: \"automatic\",\n    action,\n    authorId: positiveInteger(value.authorId, \"review job.trigger.authorId\"),\n    senderId: positiveInteger(value.senderId, \"review job.trigger.senderId\"),\n    baseRepositoryId,\n    headRepositoryId,\n    baseSha,\n    headSha\n  };\n}\nfunction parseRequestedTrigger(value) {\n  checkKeys(value, \"review job.trigger\", REQUESTED_TRIGGER_FIELDS);\n  if (value.source !== \"issue_comment\") {\n    throw new ReviewJobSchemaError(\"review job.trigger.source is not supported.\");\n  }\n  return {\n    kind: \"requested\",\n    source: value.source,\n    commentId: positiveInteger(value.commentId, \"review job.trigger.commentId\"),\n    senderId: positiveInteger(value.senderId, \"review job.trigger.senderId\")\n  };\n}\nfunction parseReviewQueueJob(value) {\n  const job = record(candidate(value), \"review job\");\n  checkKeys(job, \"review job\", TOP_LEVEL_FIELDS);\n  if (job.version !== REVIEW_QUEUE_JOB_CONTRACT_VERSION) {\n    throw new ReviewJobSchemaError(`review job.version must be ${REVIEW_QUEUE_JOB_CONTRACT_VERSION}.`);\n  }\n  const repository = parseRepository(job.repository);\n  const pullRequest = record(job.pullRequest, \"review job.pullRequest\");\n  checkKeys(pullRequest, \"review job.pullRequest\", PULL_REQUEST_FIELDS);\n  const trigger = record(job.trigger, \"review job.trigger\");\n  return {\n    version: REVIEW_QUEUE_JOB_CONTRACT_VERSION,\n    deliveryId: parseDeliveryId(job.deliveryId),\n    installationId: positiveInteger(job.installationId, \"review job.installationId\"),\n    repository,\n    pullRequest: {\n      number: positiveInteger(pullRequest.number, \"review job.pullRequest.number\")\n    },\n    trigger: trigger.kind === \"automatic\" ? parseAutomaticTrigger(trigger, repository.id) : trigger.kind === \"requested\" ? parseRequestedTrigger(trigger) : (() => {\n      throw new ReviewJobSchemaError(\"review job.trigger.kind is not supported.\");\n    })(),\n    enqueuedAt: parseEnqueuedAt(job.enqueuedAt)\n  };\n}\n\n// ../../packages/contracts/dist/policy.js\nvar REVOIR_POLICY_CONTRACT_VERSION = 1;\nvar REVOIR_POLICY_KV_KEY = \"policy\";\nvar PolicySchemaError = class extends Error {\n  constructor(message, options) {\n    super(message, options);\n    this.name = \"PolicySchemaError\";\n  }\n};\nvar GITHUB_OWNER2 = /^[A-Za-z0-9](?:[A-Za-z0-9-]{0,38})$/u;\nvar GITHUB_REPOSITORY2 = /^[A-Za-z0-9._-]+$/u;\nvar TOP_LEVEL_FIELDS2 = [\"version\", \"revision\", \"userId\", \"installations\"];\nvar INSTALLATION_FIELDS = [\"id\", \"repositories\"];\nvar REPOSITORY_FIELDS2 = [\"id\", \"owner\", \"name\"];\nfunction record2(value, path) {\n  if (typeof value !== \"object\" || value === null || Array.isArray(value)) {\n    throw new PolicySchemaError(`${path} must be an object.`);\n  }\n  return value;\n}\nfunction checkKeys2(value, path, expected) {\n  if (Object.keys(value).length !== expected.length || Object.keys(value).some((key) => !expected.includes(key))) {\n    throw new PolicySchemaError(`${path} must contain exactly the expected fields.`);\n  }\n}\nfunction positiveInteger2(value, path) {\n  if (!Number.isSafeInteger(value) || value <= 0) {\n    throw new PolicySchemaError(`${path} must be a positive integer.`);\n  }\n  return value;\n}\nfunction nonNegativeInteger(value, path) {\n  if (!Number.isSafeInteger(value) || value < 0) {\n    throw new PolicySchemaError(`${path} must be a non-negative integer.`);\n  }\n  return value;\n}\nfunction string2(value, path) {\n  if (typeof value !== \"string\" || value.length === 0) {\n    throw new PolicySchemaError(`${path} must be a non-empty string.`);\n  }\n  return value;\n}\nfunction candidate2(value) {\n  if (typeof value !== \"string\") {\n    return value;\n  }\n  try {\n    return JSON.parse(value);\n  } catch (error) {\n    throw new PolicySchemaError(\"Revoir policy must be valid JSON.\", { cause: error });\n  }\n}\nfunction parseRepository2(value, path) {\n  const repository = record2(value, path);\n  checkKeys2(repository, path, REPOSITORY_FIELDS2);\n  const owner = string2(repository.owner, `${path}.owner`);\n  const name = string2(repository.name, `${path}.name`);\n  if (!GITHUB_OWNER2.test(owner) || !GITHUB_REPOSITORY2.test(name)) {\n    throw new PolicySchemaError(`${path} contains a malformed GitHub repository identity.`);\n  }\n  return {\n    id: positiveInteger2(repository.id, `${path}.id`),\n    owner,\n    name\n  };\n}\nfunction parseRevoirPolicy(value) {\n  const policy = record2(candidate2(value), \"Revoir policy\");\n  checkKeys2(policy, \"Revoir policy\", TOP_LEVEL_FIELDS2);\n  if (policy.version !== REVOIR_POLICY_CONTRACT_VERSION) {\n    throw new PolicySchemaError(`Revoir policy.version must be ${REVOIR_POLICY_CONTRACT_VERSION}.`);\n  }\n  if (!Array.isArray(policy.installations)) {\n    throw new PolicySchemaError(\"Revoir policy.installations must be an array.\");\n  }\n  const installationIds = /* @__PURE__ */ new Set();\n  const repositoryIds = /* @__PURE__ */ new Set();\n  const repositoryNames = /* @__PURE__ */ new Set();\n  const installations = policy.installations.map((installationValue, installationIndex) => {\n    const path = `Revoir policy.installations[${installationIndex}]`;\n    const installation = record2(installationValue, path);\n    checkKeys2(installation, path, INSTALLATION_FIELDS);\n    const id = positiveInteger2(installation.id, `${path}.id`);\n    if (installationIds.has(id)) {\n      throw new PolicySchemaError(\"Revoir policy contains a duplicate installation ID.\");\n    }\n    installationIds.add(id);\n    if (!Array.isArray(installation.repositories)) {\n      throw new PolicySchemaError(`${path}.repositories must be an array.`);\n    }\n    const repositories = installation.repositories.map((repositoryValue, repositoryIndex) => {\n      const repository = parseRepository2(repositoryValue, `${path}.repositories[${repositoryIndex}]`);\n      const fullName = `${repository.owner}/${repository.name}`.toLowerCase();\n      if (repositoryIds.has(repository.id)) {\n        throw new PolicySchemaError(\"Revoir policy contains a duplicate repository ID.\");\n      }\n      if (repositoryNames.has(fullName)) {\n        throw new PolicySchemaError(\"Revoir policy contains a duplicate repository full name.\");\n      }\n      repositoryIds.add(repository.id);\n      repositoryNames.add(fullName);\n      return repository;\n    });\n    return { id, repositories };\n  });\n  return {\n    version: REVOIR_POLICY_CONTRACT_VERSION,\n    revision: nonNegativeInteger(policy.revision, \"Revoir policy.revision\"),\n    userId: positiveInteger2(policy.userId, \"Revoir policy.userId\"),\n    installations\n  };\n}\n\n// ../relay/src/index.ts\nvar SIGNATURE = /^sha256=([0-9a-f]{64})$/u;\nvar REVIEW_COMMAND = /^@revoirapp[\\t ]+review$/iu;\nfunction record3(value) {\n  return typeof value === \"object\" && value !== null && !Array.isArray(value) ? value : void 0;\n}\nfunction positiveInteger3(value) {\n  return Number.isSafeInteger(value) && value > 0 ? value : void 0;\n}\nfunction lowerHexBytes(value) {\n  const buffer = new ArrayBuffer(value.length / 2);\n  const bytes = new Uint8Array(buffer);\n  for (let index = 0; index < value.length; index += 2) {\n    bytes[index / 2] = Number.parseInt(value.slice(index, index + 2), 16);\n  }\n  return buffer;\n}\nasync function verifySignature(rawBody, signatureHeader, secret) {\n  const match = signatureHeader === null ? null : SIGNATURE.exec(signatureHeader);\n  if (match?.[1] === void 0 || secret.length === 0) {\n    return false;\n  }\n  const key = await crypto.subtle.importKey(\n    \"raw\",\n    new TextEncoder().encode(secret),\n    { name: \"HMAC\", hash: \"SHA-256\" },\n    false,\n    [\"verify\"]\n  );\n  return crypto.subtle.verify(\"HMAC\", key, lowerHexBytes(match[1]), rawBody);\n}\nasync function loadPolicy(environment) {\n  const value = await environment.POLICY_KV.get(REVOIR_POLICY_KV_KEY);\n  if (value === null) {\n    throw new Error(\"Revoir policy is unavailable.\");\n  }\n  return parseRevoirPolicy(value);\n}\nfunction repositoryPolicy(repository, installationId, policy) {\n  const id = positiveInteger3(repository.id);\n  const owner = record3(repository.owner)?.login;\n  const name = repository.name;\n  const fullName = repository.full_name;\n  if (id === void 0 || typeof owner !== \"string\" || typeof name !== \"string\" || typeof fullName !== \"string\") {\n    return void 0;\n  }\n  return policy.installations.find((candidate3) => candidate3.id === installationId)?.repositories.find(\n    (candidate3) => candidate3.id === id && candidate3.owner.toLowerCase() === owner.toLowerCase() && candidate3.name.toLowerCase() === name.toLowerCase() && `${candidate3.owner}/${candidate3.name}`.toLowerCase() === fullName.toLowerCase()\n  );\n}\nfunction createAutomaticReviewJob(payloadValue, deliveryId, policy, now) {\n  const payload = record3(payloadValue);\n  const repository = record3(payload?.repository);\n  const pullRequest = record3(payload?.pull_request);\n  const installation = record3(payload?.installation);\n  const sender = record3(payload?.sender);\n  const author = record3(pullRequest?.user);\n  const base = record3(pullRequest?.base);\n  const head = record3(pullRequest?.head);\n  const baseRepository = record3(base?.repo);\n  const headRepository = record3(head?.repo);\n  if (payload === void 0 || repository === void 0 || pullRequest === void 0 || installation === void 0 || sender === void 0 || author === void 0 || base === void 0 || head === void 0 || baseRepository === void 0 || headRepository === void 0) {\n    return void 0;\n  }\n  const action = payload.action;\n  const installationId = positiveInteger3(installation.id);\n  if (typeof action !== \"string\" || !REVIEW_JOB_ACTIONS.includes(action) || installationId === void 0) {\n    return void 0;\n  }\n  const configuredRepository = repositoryPolicy(repository, installationId, policy);\n  const repositoryId = positiveInteger3(repository.id);\n  const pullRequestNumber = positiveInteger3(pullRequest.number);\n  const envelopeNumber = positiveInteger3(payload.number);\n  const authorId = positiveInteger3(author.id);\n  const senderId = positiveInteger3(sender.id);\n  const baseRepositoryId = positiveInteger3(baseRepository.id);\n  const headRepositoryId = positiveInteger3(headRepository.id);\n  const baseRepositoryName = baseRepository.full_name;\n  const headRepositoryName = headRepository.full_name;\n  if (configuredRepository === void 0 || repositoryId === void 0 || pullRequestNumber === void 0 || envelopeNumber !== pullRequestNumber || authorId !== policy.userId || senderId !== policy.userId || pullRequest.state !== \"open\" || pullRequest.draft !== false || baseRepositoryId !== repositoryId || headRepositoryId !== repositoryId || typeof baseRepositoryName !== \"string\" || typeof headRepositoryName !== \"string\" || baseRepositoryName.toLowerCase() !== `${configuredRepository.owner}/${configuredRepository.name}`.toLowerCase() || headRepositoryName.toLowerCase() !== baseRepositoryName.toLowerCase()) {\n    return void 0;\n  }\n  const trigger = {\n    kind: \"automatic\",\n    action,\n    authorId,\n    senderId,\n    baseRepositoryId,\n    headRepositoryId,\n    baseSha: typeof base.sha === \"string\" ? base.sha : \"\",\n    headSha: typeof head.sha === \"string\" ? head.sha : \"\"\n  };\n  try {\n    return parseReviewQueueJob({\n      version: 1,\n      deliveryId,\n      installationId,\n      repository: configuredRepository,\n      pullRequest: { number: pullRequestNumber },\n      trigger,\n      enqueuedAt: now.toISOString()\n    });\n  } catch {\n    return void 0;\n  }\n}\nfunction createRequestedReviewJob(payloadValue, deliveryId, policy, now) {\n  const payload = record3(payloadValue);\n  const repository = record3(payload?.repository);\n  const installation = record3(payload?.installation);\n  const sender = record3(payload?.sender);\n  const issue = record3(payload?.issue);\n  const pullRequest = record3(issue?.pull_request);\n  const comment = record3(payload?.comment);\n  const commentAuthor = record3(comment?.user);\n  if (payload === void 0 || repository === void 0 || installation === void 0 || sender === void 0 || issue === void 0 || pullRequest === void 0 || comment === void 0 || commentAuthor === void 0) {\n    return void 0;\n  }\n  const installationId = positiveInteger3(installation.id);\n  if (installationId === void 0) {\n    return void 0;\n  }\n  const configuredRepository = repositoryPolicy(repository, installationId, policy);\n  const pullRequestNumber = positiveInteger3(issue.number);\n  const commentId = positiveInteger3(comment.id);\n  const senderId = positiveInteger3(sender.id);\n  const commentAuthorId = positiveInteger3(commentAuthor.id);\n  if (payload.action !== \"created\" || configuredRepository === void 0 || pullRequestNumber === void 0 || commentId === void 0 || senderId !== policy.userId || commentAuthorId !== senderId || typeof comment.body !== \"string\" || !REVIEW_COMMAND.test(comment.body.trim())) {\n    return void 0;\n  }\n  const trigger = {\n    kind: \"requested\",\n    source: \"issue_comment\",\n    commentId,\n    senderId\n  };\n  try {\n    return parseReviewQueueJob({\n      version: 1,\n      deliveryId,\n      installationId,\n      repository: configuredRepository,\n      pullRequest: { number: pullRequestNumber },\n      trigger,\n      enqueuedAt: now.toISOString()\n    });\n  } catch {\n    return void 0;\n  }\n}\nfunction createWebhookRelay(now = () => /* @__PURE__ */ new Date()) {\n  return {\n    async fetch(request, environment) {\n      const url = new URL(request.url);\n      if (url.pathname !== \"/github/webhook\") {\n        return new Response(\"Not Found\", { status: 404 });\n      }\n      if (request.method !== \"POST\") {\n        return new Response(\"Method Not Allowed\", { status: 405 });\n      }\n      if (request.headers.get(\"Content-Type\")?.split(\";\", 1)[0]?.trim() !== \"application/json\") {\n        return new Response(\"Unsupported Media Type\", { status: 415 });\n      }\n      const rawBody = await request.arrayBuffer();\n      if (!await verifySignature(\n        rawBody,\n        request.headers.get(\"X-Hub-Signature-256\"),\n        environment.GITHUB_WEBHOOK_SECRET\n      )) {\n        return new Response(\"Unauthorized\", { status: 401 });\n      }\n      const event = request.headers.get(\"X-GitHub-Event\");\n      if (event !== \"pull_request\" && event !== \"issue_comment\") {\n        return new Response(\"Accepted\", { status: 202 });\n      }\n      let payload;\n      try {\n        payload = JSON.parse(new TextDecoder().decode(rawBody));\n      } catch {\n        return new Response(\"Bad Request\", { status: 400 });\n      }\n      let policy;\n      try {\n        policy = await loadPolicy(environment);\n      } catch {\n        return new Response(\"Service Unavailable\", { status: 503 });\n      }\n      const deliveryId = request.headers.get(\"X-GitHub-Delivery\");\n      const job = deliveryId === null ? void 0 : event === \"pull_request\" ? createAutomaticReviewJob(payload, deliveryId, policy, now()) : createRequestedReviewJob(payload, deliveryId, policy, now());\n      if (job === void 0) {\n        return new Response(\"Accepted\", { status: 202 });\n      }\n      try {\n        await environment.REVIEW_QUEUE.send(job, { contentType: \"json\" });\n      } catch {\n        return new Response(\"Service Unavailable\", { status: 503 });\n      }\n      return new Response(\"Accepted\", { status: 202 });\n    }\n  };\n}\nvar index_default = createWebhookRelay();\nexport {\n  createWebhookRelay,\n  index_default as default\n};\n";
